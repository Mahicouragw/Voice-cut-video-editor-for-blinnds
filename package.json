{
  "name": "voicecut-video-editor",
  "version": "1.2.0",
  "private": true,
  "description": "Accessible browser video editor with optional FFmpeg server",
  "scripts": {
    "start": "node scripts/serve.js",
    "dev": "node scripts/serve.js",
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.cjs",
    "deploy": "node scripts/deploy.js",
    "build:flutter": "bash scripts/prepare-android.sh && cd flutter_app && flutter pub get && flutter build apk --debug",
    "all": "node scripts/all.js"
  },
  "devDependencies": {
    "playwright": "1.58.2"
  }
}
